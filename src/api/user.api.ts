import apiClient from './apiClient';
import { AuthResponse } from '../types/user';

export const login = async (): Promise<AuthResponse> => {
    return {} as AuthResponse; // Temporary
};
export const register = async () => { };
export const getProfile = async () => { };